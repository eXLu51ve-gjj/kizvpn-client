package com.kizvpn.client.ui.theme

import androidx.compose.ui.graphics.Color

// Неоновая цветовая палитра для KIZ_VPN
val NeonPrimary = Color(0xFF6A00FF)      // Фиолетовый неон (основной)
val NeonAccent = Color(0xFF00E5FF)       // Бирюзовый неон (акцент)
val NeonGreen = Color(0xFF00FF88)        // Зеленый неон (для статуса)
val NeonPink = Color(0xFFFF00AA)         // Розовый неон (дополнительный)

// Темные фоны
val BackgroundDark = Color(0xFF000000)   // Самый черный фон
val SurfaceDark = Color(0xFF0D0D10)      // Темная поверхность
val CardDark = Color(0xFF151518)         // Темная карточка

// Светлые фоны (Material 3 Light с мягкими тенями)
val BackgroundLight = Color(0xFFFAFAFA)  // Светлый фон
val SurfaceLight = Color(0xFFFFFFFF)     // Белая поверхность
val CardLight = Color(0xFFFFFFFF)        // Белая карточка

// Текст (темная тема)
val TextPrimary = Color(0xFFEAEAEA)      // Основной текст
val TextSecondary = Color(0xFFAAAAAA)    // Вторичный текст
val TextHint = Color(0xFF666666)         // Подсказки

// Текст (светлая тема)
val TextPrimaryLight = Color(0xFF1C1B1F)  // Основной текст (светлая)
val TextSecondaryLight = Color(0xFF49454F) // Вторичный текст (светлая)
val TextHintLight = Color(0xFF79747E)     // Подсказки (светлая)

// Статусы
val StatusConnected = NeonGreen
val StatusDisconnected = Color(0xFFFF6B6B)
val StatusConnecting = NeonAccent

// Тени для светлой темы (мягкие)
val ShadowLight = Color(0x1A000000)      // Мягкая тень
val ShadowMedium = Color(0x33000000)     // Средняя тень

